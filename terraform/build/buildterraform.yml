version: 0.2

#env:
#  variables:
#    key: "value"
#    key: "value"

phases:
  install:
    commands:
      - wget https://releases.hashicorp.com/terraform/0.13.4/terraform_0.13.4_linux_amd64.zip
      - sudo unzip -o terraform_0.13.4_linux_amd64.zip
      - sudo mv terraform /bin
      - rm terraform_0.13.4_linux_amd64.zip
  build:
    commands:
      - terraform init
      - echo "Post apply build"
  post_build:
    commands:
      - echo "Nothing to do in the post_build for now"