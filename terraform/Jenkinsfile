pipeline {
    agent any
    stages {
        stage ('Create S3') {
            steps {
               awsCodeBuild(projectName: 'project', credentialsType: 'keys', region: 'us-west-2', sourceControlType: 'jenkins',buildSpecFile:'')
            }
        }
    }
}